import React, { useRef } from 'react';
import { StyleSheet, Text, View } from 'react-native';
import COLORS from '../../themes/theme';

const Reels = () => {
	return <View style={{ backgroundColor: COLORS.dark_blue, flex: 1 }}></View>;
};

export default Reels;
